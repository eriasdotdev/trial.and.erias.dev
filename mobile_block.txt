
> // Mobile Adjustments
  @media all and (max-width: 800px) {
    html, body, .page, .page > #quartz-body {
      height: auto;
      overflow-x: hidden; // Prevent horizontal scrolling
      overflow-y: auto; // Restore vertical scrolling!
      width: 100%;
      max-width: 100vw;
    }
  
    .page > #quartz-body {
      padding: 0;
      gap: 0;
      display: flex; // Use flex to reorder
      flex-direction: column;
      
      .sidebar.left {
         order: 1;
         // Keep existing style
         border-radius: 0;
         border: none;
         background-color: var(--light);
         padding: 1rem;
         height: auto;
         width: 100%;
         box-sizing: border-box;
      }
  
      .center {
         display: contents; // Unwrap center to allow reordering children
         // Padding must be handled by children now
      }
  
      // Children of center (now direct flex items of #quartz-body)
      .page-header {
         order: 2;
         width: 100%;
         padding: 1rem;
         box-sizing: border-box;
      }
      
      // Mobile Right Sidebar (Top Bar) -> Icons
      .sidebar.right {
         order: 3; // After header, before content
         display: flex !important;
         flex-direction: column !important;
         width: 100% !important;
         max-width: 100%;
         height: auto;
         background: transparent;
         border: none;
         padding: 0 1rem 1rem 1rem; // Add bottom padding to separate from content
         gap: 4px;
         box-sizing: border-box;
         
         .sidebar-icons {
             @include box-style;
             flex-direction: row;
             width: 100% !important;
             height: 52px;
             min-height: 52px;
             flex: 0 0 52px !important;
             padding: 0 10px;
             justify-content: space-between;
             gap: 10px;
             align-items: center;
  
             button {
                 width: auto;
                 flex: 1;
                 height: 36px;
                 border-radius: 8px;
                 &.active {
                   background: rgba(255, 255, 255, 0.1);
                   color: var(--secondary);
                 }
             }
         }
         
         .sidebar-content {
             @include box-style;
             width: 100%;
             flex: 1;
             min-height: 0;
             padding: 10px;
             
             // Ensure it pushes content down when expanded
             position: static; 
             
             // Arrange links horizontally
             ul {
                  display: flex;
                  flex-direction: row;
                  flex-wrap: wrap;
                  gap: 8px;
                  padding: 0;
                  margin: 0;
                  list-style: none;
                  
                  li {
                      margin: 0;
                  }
             }
         }
         
         // Ensure close button is visible on mobile if needed, or hide if using toggle logic
         .sidebar-view-header .sidebar-close {
             display: flex;
         }
      }
      
      // Handle Content and Footer (siblings of page-header in center)
      // Now we have a specific class for content
      
      .page-content {
         order: 4;
         width: 100%;
         padding: 0 1rem; // Add side padding for content
         box-sizing: border-box;
      }
  
      // Explicitly target hr if needed, or included in above (hr is sibling)
      hr {
          order: 4;
          width: 100%;
          margin: 2rem 0;
          border: none;
          border-top: 1px solid var(--lightgray);
      }
      
      .page-footer {
          order: 5;
          width: 100%;
          padding: 2rem 1rem;
          box-sizing: border-box;
      }
      
      & > footer {
         order: 6;
         grid-column: 1;
         grid-row: auto;
         border-radius: 0;
         border: none;
         background-color: transparent;
         padding: 2rem;
         width: 100%;
         max-width: 100%;
         box-sizing: border-box;
         
         p {
           line-height: 1.6rem; // Revert line height for mobile

